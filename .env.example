# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Service role (server-only, for public form submissions). From Dashboard > API > service_role
SUPABASE_SERVICE_ROLE_KEY=

# Email (Resend) - new quote request notifications
RESEND_API_KEY=
FROM_EMAIL=onboarding@resend.dev
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Worker board Web Push (generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
# Public key for client - copy from VAPID_PUBLIC_KEY
NEXT_PUBLIC_VAPID_PUBLIC_KEY=

# PayFast (South African payments) - https://www.payfast.co.za/
# Platform (MonoPage subscription billing) - use MONOPAGE_* or PAYFAST_* as fallback
MONOPAGE_PAYFAST_MERCHANT_ID=
MONOPAGE_PAYFAST_MERCHANT_KEY=
MONOPAGE_PAYFAST_PASSPHRASE=
MONOPAGE_PAYFAST_SANDBOX=1
# Legacy (invoice payments - now uses per-user credentials from Settings > Payments)
PAYFAST_MERCHANT_ID=
PAYFAST_MERCHANT_KEY=
PAYFAST_PASSPHRASE=
PAYFAST_SANDBOX=1

# AI: DeepSeek (recommended – cheap, large context, great for HTML/CSS/JS)
# Get a key at https://platform.deepseek.com
DEEPSEEK_API_KEY=
# Optional model: deepseek-chat (default), deepseek-reasoner
# DEEPSEEK_MODEL=deepseek-chat

# AI: Fallbacks (used when DEEPSEEK_API_KEY is not set)
# Option A – Azure OpenAI (requires deployed model)
# AZURE_OPENAI_ENDPOINT=
# AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_DEPLOYMENT_NAME=

# Option B – OpenAI API
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional model: gpt-4o-mini (default), gpt-4o, gpt-4-turbo, gpt-4, etc.
OPENAI_MODEL=gpt-4o-mini
